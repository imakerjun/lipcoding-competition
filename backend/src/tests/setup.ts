// Jest 테스트 환경 설정
beforeAll(async () => {
  // 테스트용 데이터베이스 초기화
});

afterAll(async () => {
  // 테스트 후 정리
});

beforeEach(async () => {
  // 각 테스트 전에는 특별한 정리 없음 (각 테스트가 독립적으로 데이터베이스를 관리)
});

afterEach(async () => {
  // 각 테스트 후 정리
});
